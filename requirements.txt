# Core
# NOTE: install torch separately first to control CUDA wheel:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu124
# Latest transformers requires torch >= 2.6 (CVE-2025-32434 fix for torch.load).
# PyTorch dropped cu121 wheels at 2.6 — use cu124 (or cu126 for newer drivers).
torch>=2.6.0
transformers>=4.40.0
accelerate>=0.30.0
datasets>=2.18.0
tokenizers>=0.15.0
sentencepiece>=0.2.0

# Data / metrics
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
pyyaml>=6.0
tqdm>=4.66

# Logging
tensorboard>=2.14
wandb>=0.16

# Pretrain pipeline (optional, only needed for MSP-PODCAST stage)
openai>=1.30           # GPT-3.5 Turbo pseudo-labeling
openai-whisper>=20231117   # local Whisper if you want to run it yourself
# faster-whisper>=1.0.0    # uncomment for GPU-accelerated transcription
librosa>=0.10
soundfile>=0.12

# Dev
pytest>=7.4
black>=24.0
